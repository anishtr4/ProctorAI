const panels = require('react-resizable-panels');
console.log('Exports:', Object.keys(panels));
console.log('Panel:', panels.Panel);
console.log('PanelGroup:', panels.PanelGroup);
console.log('PanelResizeHandle:', panels.PanelResizeHandle);
